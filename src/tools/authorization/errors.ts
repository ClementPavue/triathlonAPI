enum Messages {
    MISSING_PRIVILEGES = "You do not have sufficient rights to perform this action, please ask your administrator",
}

enum Labels {
    MISSING_PRIVILEGES = "FORBIDDEN_MISSING_PRIVILEGES",
}

export default {
    Messages,
    Labels,
};
